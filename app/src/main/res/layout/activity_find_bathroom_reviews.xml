<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.michael.thrones.FindBathroomReviews">

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/restaurants_btn"
        android:layout_alignParentTop="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView android:id="@+id/directions"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Directions:\nClick on a place to recieve directions or click on the restaurants button to receive a map of local restaurants."
                android:textColor="#ffffff"
                android:textSize="20dp"
                android:layout_marginBottom="6dp"
                />
        </LinearLayout>
    </ScrollView>

    <Button android:id="@+id/restaurants_btn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="restaurants"
        android:textColor="#ff8300"
        android:background="#ffffff"
        android:textSize="20dp"/>
</RelativeLayout>
